#include "main.h"

/**
 * mul - returns result of multiplication of 2 numbers
 * @a: ist number
 * @b: 2nd number
 *
 * Return: the product
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
