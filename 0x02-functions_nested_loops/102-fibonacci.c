#include "main.h"
#include <stdio.h>

/**
 * main - prints the first 50 fibonacci numbers starting with 1 and 2
 *
 * Return: 0 on success
 */
int main(void)
{
	int i, j, k;

	for (i = 0; i <= 50; i++)
	{
		printf("1, ");
		printf("2, ")
