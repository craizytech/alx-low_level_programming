#include "main.h"

/**
 * prints_times_table - prints timestable of n
 * @n: the number to be checked
 */
void prints_times_table(int n)
{
	int num;
	int mult;
	int prod;

	if (n >= 0; && n <= 15)
	{
		for (num = 0; num <= n; num++)
		{
			_putchar('0');
